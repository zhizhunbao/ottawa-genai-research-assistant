/**
 * EvaluationView - 评估仪表板视图
 *
 * 视图层负责组合组件。
 * 布局（PageContainer）由 MainLayout 自动处理。
 */

import EvaluationDashboard from '../components/EvaluationDashboard'

export default function EvaluationView() {
  return <EvaluationDashboard />
}
