/**
 * Auth Layout
 *
 * Centered card layout for authentication pages (login, register, forgot password).
 *
 * @source shadcn-admin
 * @i18n Replace brand name with t() call
 *
 * @example
 * // In router config
 * { path: '/login', element: <AuthLayout><LoginForm /></AuthLayout> }
 *
 * @example
 * // Wrap auth forms
 * <AuthLayout>
 *   <SignInForm />
 * </AuthLayout>
 */

// Replace with your logo component
// import { Logo } from '{{alias}}/assets/logo'

type AuthLayoutProps = {
  children: React.ReactNode
}

export function AuthLayout({ children }: AuthLayoutProps) {
  return (
    <div className="container grid h-svh max-w-none items-center justify-center">
      <div className="mx-auto flex w-full flex-col justify-center space-y-2 py-8 sm:w-[480px] sm:p-8">
        <div className="mb-4 flex items-center justify-center">
          {/* <Logo className="me-2" /> */}
          <div className="h-8 w-8 rounded-md bg-primary" /> {/* Placeholder logo */}
          <h1 className="ms-2 text-xl font-medium">App Name</h1>
        </div>
        {children}
      </div>
    </div>
  )
}

