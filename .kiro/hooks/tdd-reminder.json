{
  "name": "TDD Reminder on New File",
  "version": "1.0.0",
  "description": "Remind to write tests first when creating new Python files",
  "when": {
    "type": "fileCreated",
    "patterns": ["*.py"]
  },
  "then": {
    "type": "askAgent",
    "prompt": "A new Python file was created. Remind me to follow TDD workflow:\n\n1. Write tests FIRST (RED)\n2. Then implement minimal code (GREEN)\n3. Then refactor (REFACTOR)\n\nAsk if I want to create the corresponding test file now."
  }
}
